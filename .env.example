# ==============================================
# CompanyOS - Production Environment Template
# ==============================================
# 
# SECURITY WARNING: This is a template file!
# - DO NOT use these example values in production
# - Generate secure random secrets before deployment
# - Never commit real credentials to version control
# - Copy this file to .env and update with real values
#
# ==============================================

# ==============================================
# DATABASE (REQUIRED)
# ==============================================
DATABASE_URL=postgresql://username:password@localhost:5432/companyos

# Individual PostgreSQL connection parameters (auto-set by Replit)
PGHOST=localhost
PGPORT=5432
PGDATABASE=companyos
PGUSER=username
PGPASSWORD=password

# ==============================================
# SESSION SECURITY (REQUIRED - CRITICAL!)
# ==============================================
# Generate a secure random secret with:
#   openssl rand -base64 32
# OR:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#
# ⚠️  SECURITY CRITICAL: Application will NOT start without this!
# ⚠️  NEVER use the example value below - generate your own!
SESSION_SECRET=REPLACE-THIS-WITH-SECURE-RANDOM-STRING-MIN-32-CHARS

# ==============================================
# COMPANY BRANDING
# ==============================================
COMPANY_NAME=Creative Code Nexus
COMPANY_TAGLINE=Digital Solutions & Innovation

# ==============================================
# SERVER CONFIGURATION
# ==============================================
NODE_ENV=production
PORT=5000

# CORS - Comma-separated allowed origins (production only)
# Example: https://yourdomain.com,https://app.yourdomain.com
ALLOWED_ORIGINS=https://yourdomain.com

# ==============================================
# OPTIONAL: REPLIT INTEGRATION
# ==============================================
# Only needed if deploying on Replit platform
# These are automatically set by Replit in their environment
# REPL_ID=your-repl-id
# ISSUER_URL=https://replit.com/oidc
# REPLIT_DOMAINS=your-domain.replit.app

# ==============================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ==============================================
# Before deploying to production:
#
# 1. ✅ Generate unique SESSION_SECRET (use openssl command above)
# 2. ✅ Update DATABASE_URL with production database credentials
# 3. ✅ Set NODE_ENV=production
# 4. ✅ Configure ALLOWED_ORIGINS with your production domain(s)
# 5. ✅ Update COMPANY_NAME and COMPANY_TAGLINE
# 6. ✅ Enable HTTPS (required for secure cookies)
# 7. ✅ Configure database backups
# 8. ✅ Set up firewall rules
# 9. ✅ Never commit .env file to version control
# 10. ✅ Use environment variables in hosting platform (Heroku, DigitalOcean, etc.)
#
# SECURITY FEATURES ENABLED:
# ✓ PostgreSQL-backed session storage (persistent across restarts)
# ✓ CSRF protection on all POST/PATCH/DELETE routes
# ✓ HTTP-only cookies (prevents XSS attacks)
# ✓ SameSite cookie protection
# ✓ Secure cookies in production (HTTPS only)
# ✓ Session timeout (7 days)
# ✓ Bcrypt password hashing
# ✓ Environment-based secrets (no hardcoded credentials)